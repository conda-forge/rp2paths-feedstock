{% set name = "rp2paths" %}
{% set version = "2.1.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/brsynth/{{ name }}/archive/{{ version }}.tar.gz
  sha256: 6157fb0c1b8817cf32d40f2af6fc9734f9a3f1ea99debc23d5411b55e87314cb

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install -vv . --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - rdkit <2024.09
    - openjdk
    - pandas
    - pydotplus
    - graphviz
    - python-graphviz
    - lxml
    - brs_utils >=1.24.0

test:
  source_files:
    - tests
  imports:
    - rp2paths
  requires:
    - python {{ python_min }}
  commands:
    - python -m rp2paths --help

about:
  home: https://github.com/brsynth/rp2paths
  summary: RetroPath2.0 to pathways
  license: MIT
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - tduigou
    - breakthewall
