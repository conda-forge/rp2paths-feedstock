{% set name = "rp2paths" %}
{% set version = "2.1.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/brsynth/{{ name }}/archive/{{ version }}.tar.gz
  sha256: e2bd293cd2a4e312886ffee7a376c05a0857f9af0baf928372006b5cd9d19b44

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install -vv . --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - rdkit <2024.09
    - openjdk
    - pandas
    - pydotplus
    - graphviz
    - python-graphviz
    - lxml
    - brs_utils >=1.24.0

test:
  source_files:
    - tests
  imports:
    - rp2paths
  requires:
    - python {{ python_min }}
  commands:
    - python -m rp2paths --help

about:
  home: https://github.com/brsynth/rp2paths
  summary: RetroPath2.0 to pathways
  license: MIT
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - tduigou
    - breakthewall
